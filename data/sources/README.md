# Sources

Store source manifests, extraction logs, source URLs, and license notes.

Relevant local collections:
- `stpt-live` for live public transport probes
- `timisoara-open-data` for municipal mobility datasets
- `timisoara-road-closures` for official closure and restriction notices
